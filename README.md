# demo-calc

A tiny calculator utility used as a reference project for agent workflows.

## Development

- Source: `src/calc.py`
- Tests: `tests/test_calc.py` — run with `python3 -m pytest tests/ -q` (or unittest)

See `AGENTS.md` for workspace conventions required by CI.
